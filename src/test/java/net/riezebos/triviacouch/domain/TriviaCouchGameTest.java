package net.riezebos.triviacouch.domain;

import static org.junit.jupiter.api.Assertions.*;

import org.junit.jupiter.api.Test;

import net.riezebos.triviacouch.resource.IDUtil;

class TriviaCouchGameTest {

	@Test
	public SpelSessie genereerTestSessie() {
		/*
		TriviaCouchGame couchGame = new TriviaCouchGame();
		
		IDUtil idGenerator = new IDUtil();
		long sessieID = idGenerator.getNextId();

		SpelSessie spelSessie = new SpelSessie();

		System.out.println("Het sessieID om te joinen!" + sessieID);
		if (!couchGame.maakSessie(sessieID)) {
			spelSessie.setSessieID(sessieID);
			spelsessieLijst.add(spelSessie);
			spelSessie.setup();
		} else
			return null;

		return spelSessie;
		*/
		return null;
	}

}
