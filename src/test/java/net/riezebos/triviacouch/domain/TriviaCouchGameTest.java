package net.riezebos.triviacouch.domain;

import org.junit.jupiter.api.Test;

class TriviaCouchGameTest {

	@Test
	public SpelSessie genereerTestSessie() {
		/*
		 * TriviaCouchGame couchGame = new TriviaCouchGame();
		 * 
		 * IDUtil idGenerator = new IDUtil(); long sessieID = idGenerator.getNextId();
		 * 
		 * SpelSessie spelSessie = new SpelSessie();
		 * 
		 * System.out.println("Het sessieID om te joinen!" + sessieID); if
		 * (!couchGame.maakSessie(sessieID)) { spelSessie.setSessieID(sessieID);
		 * spelsessieLijst.add(spelSessie); spelSessie.setup(); } else return null;
		 * 
		 * return spelSessie;
		 */
		return null;
	}

}
